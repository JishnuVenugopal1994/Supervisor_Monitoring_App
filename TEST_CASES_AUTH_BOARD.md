# Test Cases — Authentication, Navigation, Board

Part 1 of 2 detailed test case files. See `TEST_CASES.md` for the master index, prerequisites, and seed data.

**Suites in this file:** TC-AUTH (9 cases) · TC-NAV (3 cases) · TC-BOARD-SUP (17 cases) · TC-BOARD-VIW (3 cases)

---

## TC-AUTH — Authentication (9 cases)

| ID | Test Name | Type | Role | Preconditions | Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|---|
| TC-AUTH-01 | Valid supervisor login | Positive | SUP | App running; on `/login` | 1. `browser_navigate` → `http://localhost:5173/login` 2. `browser_type` `input[name="username"]` → `supervisor` 3. `browser_type` `input[name="password"]` → `password123` 4. `browser_click` `button >> text="Sign In"` | URL changes to `/board`; no error shown | High |
| TC-AUTH-02 | Valid viewer login | Positive | VWR | App running; on `/login` | 1. `browser_type` `input[name="username"]` → `viewer` 2. `browser_type` `input[name="password"]` → `viewer123` 3. `browser_click` `button >> text="Sign In"` | URL changes to `/board` | High |
| TC-AUTH-03 | Wrong password shows error toast | Negative | SUP | On `/login` | 1. `browser_type` username → `supervisor` 2. `browser_type` password → `wrongpassword` 3. `browser_click` `"Sign In"` | Error toast appears with text `"Invalid credentials"`; URL stays `/login`; form still visible | High |
| TC-AUTH-04 | Empty fields — client validation | Negative | ANY | On `/login` | 1. Leave both fields empty 2. `browser_click` `button >> text="Sign In"` | Inline error text `"Username and password are required"` visible below form; no API call made; URL stays `/login` | High |
| TC-AUTH-05 | Sign In button loading state | Edge | SUP | On `/login`; valid credentials filled | 1. Fill `supervisor` / `password123` 2. `browser_click` `"Sign In"` 3. Immediately `browser_snapshot` | Button text = `"Signing in…"`; button has `disabled` attribute; after response, button reverts to `"Sign In"` | Medium |
| TC-AUTH-06 | Logout clears session and redirects | Security | SUP | Logged in as supervisor; on `/board` | 1. `browser_click` `button >> text="Logout"` 2. Assert URL = `/login` 3. `browser_navigate` → `http://localhost:5173/board` | After logout: URL = `/login`; navigating to `/board` redirects back to `/login` (ProtectedRoute) | High |
| TC-AUTH-07 | Already logged in redirected away from /login | Edge | SUP | Already logged in as supervisor | 1. `browser_navigate` → `http://localhost:5173/login` | Immediately redirected to `/board`; login form is never rendered | Medium |
| TC-AUTH-08 | Page refresh restores session via refresh cookie | Edge | SUP | Logged in as supervisor; on `/board` | 1. `browser_evaluate` → `location.reload()` 2. Wait for page to settle; `browser_snapshot` | Page loads `/board` successfully without redirecting to `/login` (httpOnly refresh cookie restores access token silently) | Medium |
| TC-AUTH-09 | Expired refresh token forces logout | Negative | SUP | Logged in as supervisor | 1. Use Playwright `context.clearCookies([{ name: 'refreshToken' }])` from Node.js (httpOnly cookies cannot be cleared via JS `document.cookie`) 2. Use `page.route()` to intercept the first non-auth API request and return `401` (forces the refresh code path) 3. `browser_navigate` → `/work-orders` | Redirect to `/login`; the refresh call returns 401 (cookie gone) → axios interceptor calls `window.location.href = '/login'` | Medium |

---

## TC-NAV — Navigation (3 cases)

| ID | Test Name | Type | Role | Preconditions | Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|---|
| TC-NAV-01 | All nav links route to correct pages | Positive | ANY | Logged in; on `/board` | 1. `browser_click` `a >> text="Work Orders"` → assert URL = `/work-orders` 2. `browser_click` `a >> text="Resources"` → assert URL = `/resources` 3. `browser_click` `a >> text="Materials"` → assert URL = `/materials` 4. `browser_click` `a >> text="Board"` → assert URL = `/board` | Each nav click updates URL to the correct path | High |
| TC-NAV-02 | Header shows username and role badge | Positive | BOTH | Login as supervisor, then re-test as viewer | 1. Login as supervisor → `browser_snapshot` header area 2. Logout 3. Login as viewer → `browser_snapshot` header area | SUP header: contains text `supervisor` and badge `SUPERVISOR`. VWR header: contains text `viewer` and badge `VIEWER` | Medium |
| TC-NAV-03 | Unauthenticated access redirects to /login | Security | — | Not logged in (fresh session) | 1. `browser_navigate` → `/board` → assert URL 2. `browser_navigate` → `/work-orders` → assert URL 3. `browser_navigate` → `/resources` → assert URL 4. `browser_navigate` → `/materials` → assert URL | Every URL redirects to `/login`; protected page content is never rendered | High |

---

## TC-BOARD-SUP — Board Page — Supervisor (17 cases)

| ID | Test Name | Type | Role | Preconditions | Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|---|
| TC-BOARD-SUP-01 | Initial board state loads without errors | Positive | SUP | Logged in; DB seeded | 1. `browser_navigate` → `/board` 2. `browser_snapshot` | 5 work order columns (`[data-testid="work-order-column"]`); ResourcePanel with `"Assign to work order"` `<select>` visible on left side; `[data-testid="idle-operators"]` count ≥ 1; `[data-testid="idle-machines"]` count ≥ 1; no ErrorBoundary `"Something went wrong"` visible | High |
| TC-BOARD-SUP-02 | Only non-COMPLETED orders shown as columns | Positive | SUP | DB seeded (all 5 WOs are non-COMPLETED) | 1. `browser_navigate` → `/board` 2. Count `[data-testid="work-order-column"]` elements | Exactly 5 columns; none with a COMPLETED order's header | High |
| TC-BOARD-SUP-03 | Assign available operator to work order | Positive | SUP | DB seeded; on `/board` | 1. `browser_select_option` on `"Assign to work order"` `<select>` → select `WO-001` 2. `browser_click` `[data-testid="resource-operator"]` row containing text `"Alice Nguyen"` | `[data-testid="allocation-card"]` with text `"Alice Nguyen"` appears in `[data-testid="work-order-column"]` for WO-001; toast `"Resource assigned"`; `[data-testid="idle-operators"]` count decreases by 1 | High |
| TC-BOARD-SUP-04 | Assigned operator becomes non-clickable in ResourcePanel | Edge | SUP | TC-BOARD-SUP-03 complete (Alice assigned) | 1. `browser_evaluate` → `document.querySelector('[data-testid="resource-operator"]')?.classList.toString()` on Alice's row 2. `browser_click` Alice's row again | Class includes `cursor-not-allowed`; no second AllocationCard created; no additional API call | Medium |
| TC-BOARD-SUP-05 | Assign available machine to work order | Positive | SUP | DB seeded; on `/board` | 1. `browser_select_option` → select `WO-003` from dropdown 2. `browser_click` `[data-testid="resource-machine"]` row containing `"Welding Robot Arm 1"` | AllocationCard with `"Welding Robot Arm 1"` in WO-003 column; toast `"Resource assigned"`; `[data-testid="idle-machines"]` count decreases by 1 | High |
| TC-BOARD-SUP-06 | MAINTENANCE machine non-clickable in ResourcePanel | Edge | SUP | DB seeded; `CNC Mill Alpha` (MCH-002) is MAINTENANCE | 1. `browser_evaluate` → check `CNC Mill Alpha` row classes 2. `browser_click` its row | Row has `cursor-not-allowed` class; no AllocationCard created; no API call | Medium |
| TC-BOARD-SUP-07 | Filter operators by skill | Positive | SUP | DB seeded; on `/board` | 1. `browser_select_option` on skill filter `<select>` (next to "Operators" label) → select `"welding"` 2. `browser_snapshot` ResourcePanel 3. `browser_select_option` → select `""` (All skills) 4. `browser_snapshot` | With `"welding"` filter: Alice Nguyen, David Osei, Frank Müller visible; Bob Carter, Carmen Silva, Elena Petrova NOT visible. After reset: all 6 operators visible | Medium |
| TC-BOARD-SUP-08 | Move allocation to different column via drag | Positive | SUP | At least one AllocationCard exists in WO-001 column | 1. `browser_drag` `[data-testid="allocation-card"]` inside WO-001 column → `[data-testid="work-order-column"]` for WO-003 | Card appears in WO-003 column; absent from WO-001 column; toast `"Allocation moved"` | High |
| TC-BOARD-SUP-09 | Delete allocation | Positive | SUP | At least one AllocationCard on board | 1. `browser_click` `button[title="Remove allocation"]` on any AllocationCard | Card disappears from DOM immediately (no confirm dialog); toast `"Allocation removed"`; `[data-testid="idle-operators"]` or `[data-testid="idle-machines"]` increases by 1 if card had a resource | High |
| TC-BOARD-SUP-10 | Unnamed allocation shows "Unnamed allocation" text | Edge | SUP | Create allocation via API with no `operatorId` and no `machineId` | 1. `POST /api/allocations` with `workOrderId` + `startTime` + `endTime` only 2. `browser_navigate` → `/board` | AllocationCard in the correct column shows italic text `"Unnamed allocation"` (no operator/machine name line) | Low |
| TC-BOARD-SUP-11 | Edit time — end before start shows error toast | Negative | SUP | At least one AllocationCard on board | 1. `browser_click` `[data-testid="allocation-card"] >> [title="Click to edit times"]` 2. Set `endTime` datetime input to a value before `startTime` 3. `browser_click` `button >> text="Save"` | Toast `"End time must be after start time"`; both `datetime-local` inputs remain visible (edit mode stays open); time display on card unchanged | High |
| TC-BOARD-SUP-12 | Edit time — valid range saves and closes | Positive | SUP | At least one AllocationCard on board | 1. `browser_click` `[title="Click to edit times"]` 2. Set valid `startTime` and `endTime` (end after start) 3. `browser_click` `"Save"` | `[title="Click to edit times"]` element shows updated time; `datetime-local` inputs no longer visible; no error toast | High |
| TC-BOARD-SUP-13 | Operator double-booking rejected (OPERATOR_CONFLICT) | Negative | SUP | **Re-seed DB in `beforeEach` for this test and TC-BOARD-SUP-14** (not just `beforeAll` for the file — a prior test in the suite will have created allocations that must not exist here). No existing allocations. | 1. Select WO-001; click Alice Nguyen → first AllocationCard created 2. Select WO-002 from dropdown; click Alice Nguyen again (WO-001 and WO-002 time windows overlap) | Error toast containing `"OPERATOR_CONFLICT"`; no second AllocationCard in WO-002 column | High |
| TC-BOARD-SUP-14 | Machine double-booking rejected (MACHINE_CONFLICT) | Negative | SUP | **Re-seed DB in `beforeEach`** (same reason as TC-BOARD-SUP-13). No existing allocations. | 1. Select WO-001; click `"Welding Robot Arm 1"` → card created 2. Select WO-002; click `"Welding Robot Arm 1"` again | Error toast containing `"MACHINE_CONFLICT"`; no second AllocationCard in WO-002 column | High |
| TC-BOARD-SUP-15 | Assign with no work order selected silently does nothing | Edge | SUP | DB seeded; on `/board`; dropdown shows `"Select…"` (no WO chosen) | 1. Do NOT select a work order from the dropdown 2. `browser_click` any AVAILABLE operator row | No AllocationCard created; no toast; no API call; page state unchanged | Medium |
| TC-BOARD-SUP-16 | Empty board shows "No active work orders." message | Edge | SUP | All work orders must be COMPLETED status | 1. Use API helper (`e2e/helpers/api.ts`) to PATCH all 5 work orders to `COMPLETED` via direct HTTP calls with the supervisor token — do **not** use the UI for this (5 sequential transitions through the Work Orders page is too slow and fragile for a test precondition) 2. `browser_navigate` → `/board` | Text `"No active work orders."` is visible; no `[data-testid="work-order-column"]` elements exist | Low |
| TC-BOARD-SUP-17 | Time edit Cancel closes inputs without saving | Edge | SUP | At least one AllocationCard on board | 1. Note current time text in `[title="Click to edit times"]` 2. `browser_click` `[title="Click to edit times"]` 3. Change both datetime values 4. `browser_click` `button >> text="Cancel"` | `datetime-local` inputs disappear; `[title="Click to edit times"]` still shows the **original** time; no API call; no toast | Medium |

---

## TC-BOARD-VIW — Board Page — Viewer (3 cases)

| ID | Test Name | Type | Role | Preconditions | Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|---|
| TC-BOARD-VIW-01 | ResourcePanel is absent for viewer | Security | VWR | Logged in as viewer; on `/board` | 1. `browser_navigate` → `/board` 2. `browser_snapshot` | No `<select>` with label `"Assign to work order"` exists in DOM; no `[data-testid="resource-operator"]` or `[data-testid="resource-machine"]` elements present | High |
| TC-BOARD-VIW-02 | Delete button absent on AllocationCards for viewer | Security | VWR | Logged in as viewer; at least one AllocationCard exists | 1. `browser_snapshot` | `button[title="Remove allocation"]` does not exist anywhere in the DOM | High |
| TC-BOARD-VIW-03 | Allocation cards have no drag handles for viewer | Security | VWR | Logged in as viewer; at least one AllocationCard exists | 1. `browser_evaluate` → `document.querySelector('[data-testid="allocation-card"]')?.classList.contains('cursor-grab')` | Returns `false`; `[data-testid="idle-operators"]` and `[data-testid="idle-machines"]` badges are still visible (read-only state correct) | Medium |
