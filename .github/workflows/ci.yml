name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  smoke:
    name: Smoke test
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow triggered successfully"
